{% macro blogcard(params) %}

  {% set img800 = "/assets/img/blogposts/800x600/" ~ params.image %}

  <article class="c-blogcard">
    {% if params.image %}
      <div class="c-blogcard__media">
        <a 
          href="{{ params.url }}" 
          class="c-blogcard__medialink">
            <img 
              src="{{ img800 }}"
              class="c-blogcard__image  o-fluidimg"
              width="800"
              height="600"
              loading="lazy"
              decoding="async"
              alt="{{ params.imageAlt }}">
        </a>
      </div>
    {% endif %}
    <div class="c-blogcard__body">
      <h3 class="c-blogcard__title">
        <a href="{{ params.url }}">
          {{ params.title }}
        </a>
      </h3>
      <p class="c-blogcard__date">
        <time datetime="{{ params.date|dateISO }}">{{ params.date|dateFull }}</time>
      </p>
    </div>
  </article>

{% endmacro %}