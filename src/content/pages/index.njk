---
permalink: /index.html
---

{% extends "layouts/base.njk" %}

{% from "components/blogcard.njk" import blogcard %}

{% set metaTitle = "Webstoemp" %}
{% set metaDescription = "Webstoemp is the portfolio and blog of Jérôme Coupé, belgian designer, developer and teacher based in Brussels." %}
{% set metaImage = "" %}
{% set currentSection = "home" %}

{% set colorTheme="cyan" %}
{% set headerBackground= true %}

{% block content %}

  <div class="c-pagesection  c-pagesection--highlight">
    <div class="l-container">

      <div class="c-homeintro">
        <h1 class="c-homeintro__title">
          <span>Delicious</span>
          <span>websites</span>
        </h1>
        <h2 class="c-homeintro__subtitle">from pixels to code</h2>
      </div>

    </div>
  </div>

  {% include "components/homebanner.njk" %}

  <div class="c-pagesection">
    <div class="l-container">

      <div class="c-pagesection__block">

        <div class="l-grid" style="--gap-md: var(--grid-gap); --columns-md: 2;">
          <div>
            <h2 class="c-smalltitle">Meet the cook</h2>
          </div>
          <div>
            <p><a href="#">Webstoemp</a> is the online kitchen of Jérôme Coupé, belgian designer, developer and teacher. When I am not busy designing, coding up a website or warping young minds at school, chances are I am drinking a few Belgian craft beers with friends somewhere.</p>
            <p>Although I work on web apps too, I confess having a soft spot for publishing platforms and content-oriented websites.</p>
            <p><a class="c-ctalink" href="#">More about me</a></p>
          </div>
        </div>

      </div>
      <div class="c-pagesection__block">
        
        <h2 class="c-smalltitle">Latest blogposts</h2>
        {% set items = collections.blogposts | limit(4) %}
        {% for item in items %}
          {% if loop.first %}<ul class="l-grid" style="--gap: var(--grid-gap); --columns-md: 2; --columns-lg: 4;">{% endif %}
            <li>
              {{ blogcard({
                title: item.data.title,
                url: item.url,
                image: item.data.image,
                date: item.date
              }) }}
            </li>
          {% if loop.last %}</ul>{% endif %}
        {% endfor %}

        <p class="u-mbs-md"><a class="c-ctalink" href="#">All blogposts</a></p>

      </div>

    </div>
  </div>
  
{% endblock %}