---
permalink: /index.html
---

{% extends "layouts/base.njk" %}

{% from "components/blogcard.njk" import blogcard %}

{% set metaTitle = "Home" %}
{% set metaDescription = "Portfolio and blog of Jérôme Coupé, belgian designer, developer and teacher based in Brussels." %}
{% set metaImage = "" %}
{% set currentSection = "home" %}

{% set colorTheme ="green" %}

{% block content %}

  {# include "components/homecollage.njk" #}
  {# include "components/homebanner.njk" #}
  <div class="c-section">
    <div class="l-container">

      <h2 class="c-smalltitle u-mbe-xs">Manifesto</h2>
      <p class="c-manifesto">Like great dishes, I believe websites should be made of <em>simple &amp; well prepared</em> ingredients.</p>

    </div>
  </div>

  <div class="c-section">
    <div class="l-container">

      <div class="l-split l-split--1fr2fr">
        <div>
          <img src="/assets/img/homepictures/homepicture-square.jpg" 
               class="o-fluidimg" 
               alt="">
        </div>
        <div>
          <img src="/assets/img/homepictures/homepicture-4by3.jpg" 
               class="o-fluidimg" 
               alt="">
        </div>
      </div>

    </div>
  </div>

  <div class="c-section">
    <div class="l-container">

      <div class="l-split l-split--1fr2fr@medium">
        <div>
          <h2 class="c-smalltitle">Meet the cook</h2>
        </div>
        <div>
          <div class="u-columns-36ch">
            <p>Webstoemp is the internet home of Jérôme Coupé, designer, front-end developer from Brussels, Belgium. When I am not coding a website or teaching at school, chances are I am enjoying a few local craft beer with friends.</p>
            <p>Although I work on web apps too, I have a soft spot for building content-oriented websites. To me, Internet will always be a way to share information before being an application runtime.</p>
          </div>
          <p><a class="c-ctalink" href="#">More about me</a></p>
        </div>
      </div>

    </div>
  </div>
    
    <div class="c-section">
      <div class="l-container">
        <h2 class="c-smalltitle">Latest blogposts</h2>
        {% set items = collections.blogposts | limit(4) %}
        {% for item in items %}
          {% if loop.first %}<ul class="l-grid l-grid--2cols@small l-grid--4cols@large">{% endif %}
            <li>
              {{ blogcard({
                title: item.data.title,
                url: item.url,
                image: item.data.image,
                date: item.date
              }) }}
            </li>
          {% if loop.last %}</ul>{% endif %}
        {% endfor %}
        <p class="u-mbs-md"><a class="c-ctalink" href="#">All blogposts</a></p>
      </div>
    </div>
  
{% endblock %}